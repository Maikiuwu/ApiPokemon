package com.pokemon.guess.repository;

/* 
public interface UsedPokemonRepository extends JpaRepository<PokemonFound, PokemonFoundId> {
    boolean existsByIdUserIdAndIdPokemonId(Long userId, Integer pokemonId);
    void deleteByIdUserId(Long userId);
}
*/
